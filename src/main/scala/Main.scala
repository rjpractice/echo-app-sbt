
/**
 * Entry point of the application.
 *
 * This minimal `main` function demonstrates how to define the entry point in Scala 3 using the `@main` annotation.
 * When run, it prints a single line of text to the standard output.
 *
 * <h2>Usage:</h2>
 * Compile and run the program using `scala-cli`, `sbt`, or a Scala 3-compatible tool.
 *
 * <h3>Output:</h3>
 * {{{
 * The smell... it's coming from the sea.
 * }}}
 */
@main def main(): Unit =
  println("The smell... it's coming from the sea.")
